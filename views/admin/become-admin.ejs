<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 to-gray-800 p-6 text-white">
  <div class="max-w-lg w-full bg-white/5 backdrop-blur rounded-2xl shadow-2xl p-8 border border-white/10">
    
    <h2 class="text-3xl font-bold text-indigo-300 mb-4 text-center">🔑 Ascend to the Inner Circle</h2>
    <p class="italic text-gray-300 mb-6 text-center">“The words beneath your feet may hold the verse—catch them before they fade.”</p>

    <% if (errors.length > 0) { %>
      <ul class="mb-4 text-red-400 list-disc list-inside">
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
        <% }) %>
      </ul>
    <% } %>

    <% if (success.length > 0) { %>
      <div class="bg-green-600 text-white p-2 mb-4 rounded text-center shadow"><%= success[0] %></div>
    <% } %>

    <form action="/become-admin" method="POST" class="space-y-4">
      <input type="text" name="adminCode" autocomplete="off" placeholder="Enter the Secret Verse..." 
        class="w-full px-4 py-2 rounded-lg bg-gray-900 border border-gray-700 text-white focus:ring-2 focus:ring-indigo-500" required>

      <button type="submit" class="w-full py-2 px-4 bg-indigo-700 hover:bg-indigo-800 rounded-lg shadow-md font-semibold tracking-wide">
        Unlock the Seat of Scribes
      </button>
    </form>

    <div class="mt-8 border-t border-gray-700 pt-6 text-sm text-gray-300">
      <h3 class="text-indigo-200 text-center font-semibold mb-4 tracking-wide">🕯 Why Become an Admin?</h3>
      <ul class="space-y-3">
        <li class="flex items-start gap-2">
          <span class="text-indigo-400">🗑️</span>
          <span>Preside over the manuscripts—edit, remove, or elevate the words that dwell here.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="text-indigo-400">👁️</span>
          <span>Witness every name and every verse, visible or veiled to others.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="text-indigo-400">⚖️</span>
          <span>Hold the sacred balance between freedom of voice and the harmony of the circle.</span>
        </li>
      </ul>
    </div>

  </div>
</div>
