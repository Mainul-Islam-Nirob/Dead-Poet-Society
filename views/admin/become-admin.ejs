<div class="max-w-lg mx-auto mt-10 p-6 rounded-xl shadow-xl bg-gray-900 text-white">
  <h2 class="text-2xl font-bold text-indigo-300 mb-4">🔑 Become an Admin</h2>

  <% if (errors.length > 0) { %>
    <ul class="mb-4 text-red-400 list-disc list-inside">
      <% errors.forEach(error => { %>
        <li><%= error.msg %></li>
      <% }) %>
    </ul>
  <% } %>

  <form action="/become-admin" method="POST" class="space-y-4">
    <input type="text" name="adminCode" placeholder="Enter Secret Code - May be Found Somewhere (Not Exact)" class="w-full p-2 rounded-lg bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-400" required>
    <button type="submit" class="w-full py-2 bg-indigo-700 hover:bg-indigo-800 rounded-lg font-semibold">Become Admin</button>
  </form>
</div>
